{
  "name": "fashion-ecommerce",
  "private": true,
  "description": "Root scripts for production build (e.g. Render)",
  "scripts": {
    "build": "npm ci --prefix client && npm run build --prefix client && npm ci --prefix server",
    "start": "node server/src/app.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
